// ---------------------------------------------------------------
// HabitBubbles configuration
//
// HabitBubbles is local-only. It stores everything in this browser
// and has no backend, no sign-in, and no cross-device sync.
//
// DATA_ID namespaces this app's browser storage. It MUST differ from
// every other app served from the same origin — ChoreBubbles Solo and
// ChoreBubbles both live at jwatson7399.github.io, and localStorage is
// scoped to origin, not path.
// ---------------------------------------------------------------

export const SUPABASE_URL = "";
export const SUPABASE_ANON_KEY = "";
export const DATA_ID = "habitbubbles-local";
